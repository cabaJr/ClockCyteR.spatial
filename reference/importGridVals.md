# Import all values from kernel extraction

Import all values from kernel extraction

## Usage

``` r
importGridVals(path)
```

## Arguments

- path:

  file path of the grid coordinates to import

## Value

A numeric matrix with ROI means as rows and frames as columns. Row names
are ROI labels (with "Mean" stripped), column names are sequential frame
indices.
