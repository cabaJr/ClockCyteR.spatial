# Translate a plot-type key to its output filename stem

Translate a plot-type key to its output filename stem

## Usage

``` r
plot_type_to_name(plot_type)
```

## Arguments

- plot_type:

  Character; plot-type key.

## Value

Character; the corresponding filename stem, or `NA` if `plot_type` is
not in the dictionary.
