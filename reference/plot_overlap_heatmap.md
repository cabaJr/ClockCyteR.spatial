# Cluster segregation heatmap

Cluster segregation heatmap

## Usage

``` r
plot_overlap_heatmap(overlap_matrix)
```

## Arguments

- overlap_matrix:

  Square numeric matrix of pairwise cluster segregation scores; row and
  column names are cluster labels.

## Value

A `ggplot` object.
