# Normalise phases to hours centred around zero

Normalise phases to hours centred around zero

## Usage

``` r
normalize_phase(angles)
```

## Arguments

- angles:

  Numeric vector or circular object of phase values in radians.

## Value

Numeric vector of phase values in hours, centred around zero in the
range `[-12, 12]`.
