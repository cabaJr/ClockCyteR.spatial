# Jitter plot of per-cell period lengths

Jitter plot of per-cell period lengths

## Usage

``` r
period_print(data, ...)
```

## Arguments

- data:

  Data frame with a `period` column (numeric, in hours).

- ...:

  Currently unused.

## Value

A `ggplot` object.
