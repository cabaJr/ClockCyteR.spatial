# Mean ± SE trace plot for each group

Mean ± SE trace plot for each group

## Usage

``` r
plot_group_traces(summary_data)
```

## Arguments

- summary_data:

  Data frame with columns `t` (time), `mean`, `se`, and `group` (factor
  or character grouping variable).

## Value

A `ggplot` object.
