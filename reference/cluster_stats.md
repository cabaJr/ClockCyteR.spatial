# Calculate statistics on each cluster

Calculate statistics on each cluster

## Usage

``` r
cluster_stats(node_data)
```

## Arguments

- node_data:

  table containing period analysis in each node
