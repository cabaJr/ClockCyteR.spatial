# summarise node data

summarise node data

## Usage

``` r
summarise_cluster_data(node_data)
```

## Arguments

- node_data:

  Data frame of node-level data including columns `cluster`,
  `phase_crn`, `period_crn`, `amplitude`, `period`, `clust_coef`,
  `degree`, and `strength`.
