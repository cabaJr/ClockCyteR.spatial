# Coerce circular phase columns to numeric in a summary table

Coerce circular phase columns to numeric in a summary table

## Usage

``` r
normalize_summary(df)
```

## Arguments

- df:

  Data frame with columns `phase_rad_mean` and `circ_phase_mean`.

## Value

The input data frame with the two phase columns cast to `numeric`.
