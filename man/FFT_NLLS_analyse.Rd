% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processing_foo.R
\name{FFT_NLLS_analyse}
\alias{FFT_NLLS_analyse}
\title{Estimate period, phase, and amplitude using FFT initialisation and NLLS fitting}
\usage{
FFT_NLLS_analyse(data, minPer = 16, maxPer = 32, smooth = TRUE, invert = FALSE)
}
\arguments{
\item{data}{Data frame with columns \code{t} (time in hours), \code{value}
(raw fluorescence), and \code{smooth} (smoothed fluorescence).}

\item{minPer}{Numeric; lower period bound in hours. Defaults to \code{16}.}

\item{maxPer}{Numeric; upper period bound in hours. Defaults to \code{32}.}

\item{smooth}{Logical; if \code{TRUE}, fits the smoothed trace; if
\code{FALSE}, fits the raw trace. Defaults to \code{TRUE}.}

\item{invert}{Logical; if \code{TRUE}, inverts the signal before fitting.
Defaults to \code{FALSE}.}
}
\value{
A named list with fitted \code{period}, \code{phase},
\code{amplitude}, \code{RAE}, and \code{fitted_trace}.
}
\description{
Estimate period, phase, and amplitude using FFT initialisation and NLLS fitting
}
\keyword{internal}
