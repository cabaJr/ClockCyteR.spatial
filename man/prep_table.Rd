% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processing_foo.R
\name{prep_table}
\alias{prep_table}
\title{Reshape and optionally preprocess a trace matrix into long format}
\usage{
prep_table(
  data,
  rm.start = 0,
  preprocess = TRUE,
  transp = TRUE,
  add_t = TRUE,
  time_res
)
}
\arguments{
\item{data}{Numeric matrix of fluorescence traces (cells × frames), with
an optional leading ID column.}

\item{rm.start}{Integer; number of initial time points to remove. Defaults
to \code{0}.}

\item{preprocess}{Logical; if \code{TRUE}, calls \code{preprocess_data()}
to smooth and detrend. Defaults to \code{TRUE}.}

\item{transp}{Logical; if \code{TRUE}, transposes \code{data} before
processing. Defaults to \code{TRUE}.}

\item{add_t}{Logical; if \code{TRUE}, adds a time column based on
\code{time_res}. Defaults to \code{TRUE}.}

\item{time_res}{Numeric; temporal resolution in hours per frame.}
}
\value{
A long-format \code{data.table} with columns \code{ID}, \code{t},
\code{value}, and \code{smooth}.
}
\description{
Reshape and optionally preprocess a trace matrix into long format
}
\keyword{internal}
