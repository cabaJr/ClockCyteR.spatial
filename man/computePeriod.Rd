% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processing_foo.R
\name{computePeriod}
\alias{computePeriod}
\title{Run FFT-NLLS period analysis on a fluorescence trace matrix}
\usage{
computePeriod(
  df,
  excludeNC = FALSE,
  top = 30,
  bottom = 18,
  save.trace = FALSE,
  rm.start = 0,
  prep_tbl = TRUE,
  preprocess = TRUE,
  transp = TRUE,
  add_t = TRUE,
  time_res = 0.5,
  smooth = TRUE,
  invert = FALSE,
  filterRAE = NULL,
  ...
)
}
\arguments{
\item{df}{Numeric matrix or data frame of fluorescence traces (cells ×
frames), or a long-format table if \code{prep_tbl = FALSE}.}

\item{excludeNC}{Logical; if \code{TRUE}, excludes non-circadian cells
from the returned table. Defaults to \code{FALSE}.}

\item{top}{Numeric; upper period bound in hours. Defaults to \code{30}.}

\item{bottom}{Numeric; lower period bound in hours. Defaults to
\code{18}.}

\item{save.trace}{Logical; whether to store individual fitted traces.
Defaults to \code{FALSE}.}

\item{rm.start}{Integer; number of initial time points to discard before
analysis. Defaults to \code{0}.}

\item{prep_tbl}{Logical; if \code{TRUE}, calls \code{prep_table()} to
reshape \code{df} before analysis. Defaults to \code{TRUE}.}

\item{preprocess}{Logical; if \code{TRUE}, smooths and detrends traces
during table preparation. Defaults to \code{TRUE}.}

\item{transp}{Logical; if \code{TRUE}, transposes \code{df} during table
preparation. Defaults to \code{TRUE}.}

\item{add_t}{Logical; if \code{TRUE}, adds a time column during table
preparation. Defaults to \code{TRUE}.}

\item{time_res}{Numeric; temporal resolution in hours per frame. Defaults
to \code{0.5}.}

\item{smooth}{Logical; if \code{TRUE}, uses the smoothed trace for
analysis. Defaults to \code{TRUE}.}

\item{invert}{Logical; if \code{TRUE}, inverts the fluorescence signal
before analysis. Defaults to \code{FALSE}.}

\item{filterRAE}{Numeric threshold for the Relative Amplitude Error;
cells with RAE above this value are excluded. Pass \code{NULL} to
disable filtering. Defaults to \code{NULL}.}

\item{...}{Currently unused.}
}
\value{
A named list with elements \code{period_table} (data frame of
per-cell results), \code{period_table_unfiltered}, and optionally
\code{traces} and \code{fit_traces}.
}
\description{
Run FFT-NLLS period analysis on a fluorescence trace matrix
}
