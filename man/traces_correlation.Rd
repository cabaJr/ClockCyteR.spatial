% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_foo.R
\name{traces_correlation}
\alias{traces_correlation}
\title{collection of funtions used in network analysis
Compute pairwise cell correlations and return edge list}
\usage{
traces_correlation(
  method = "combined",
  traces,
  network_dir,
  filename,
  interval_name = "Full",
  grid_coord,
  cell_nodes,
  plot_matrix = TRUE,
  filter_nonrhythmic = FALSE
)
}
\arguments{
\item{method}{Character; correlation method, either \code{"spearman"} or
\code{"combined"} (Spearman + FFT-based rhythm similarity). Defaults to
\code{"combined"}.}

\item{traces}{Numeric matrix of cell fluorescence traces (cells × frames).}

\item{network_dir}{Character; directory for saving correlation matrix plots.}

\item{filename}{Character; file identifier used in plot filenames.}

\item{interval_name}{Character; interval label used in plot filenames.
Defaults to \code{"Full"}.}

\item{grid_coord}{Data frame of grid coordinates with columns \code{X} and
\code{Y}, used for spatial weighting in \code{"combined"} mode.}

\item{cell_nodes}{Data frame of node metadata; must contain a \code{name}
column matching row indices of \code{traces}.}

\item{plot_matrix}{Logical; whether to save a heatmap of the correlation
matrix. Defaults to \code{TRUE}.}

\item{filter_nonrhythmic}{Logical; whether to remove edges with \code{NA}
weights before returning. Defaults to \code{FALSE}.}
}
\value{
A data frame of edges with columns \code{from}, \code{to}, and
\code{weight} (Fisher z-transformed correlation).
}
\description{
collection of funtions used in network analysis
Compute pairwise cell correlations and return edge list
}
