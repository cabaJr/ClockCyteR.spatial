% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processing_foo.R
\name{phase_align_trace}
\alias{phase_align_trace}
\title{Circularly shift traces so their phases align to the group mean}
\usage{
phase_align_trace(
  traces_table,
  period_table,
  remove_start = 0,
  remove_end = 0,
  align_to = NA,
  debug = FALSE
)
}
\arguments{
\item{traces_table}{Numeric matrix of fluorescence traces (cells × frames).}

\item{period_table}{Data frame of period-analysis results with a
\code{phase_rad} column.}

\item{remove_start}{Integer; frames to discard from the start after
shifting. Defaults to \code{0}.}

\item{remove_end}{Integer; frames to discard from the end after shifting.
Defaults to \code{0}.}

\item{align_to}{Numeric; target phase in hours; if \code{NA}, uses the
circular mean of the group. Defaults to \code{NA}.}

\item{debug}{Logical; if \code{TRUE}, enables debugging output. Defaults
to \code{FALSE}.}
}
\value{
A numeric matrix of phase-aligned traces, same dimensions as
\code{traces_table} (minus any removed frames).
}
\description{
Circularly shift traces so their phases align to the group mean
}
\keyword{internal}
