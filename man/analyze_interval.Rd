% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modules_foo.R
\name{analyze_interval}
\alias{analyze_interval}
\title{analyze_interval}
\usage{
analyze_interval(file_ctx, interval_name, interval_range, params)
}
\arguments{
\item{file_ctx}{A named list of file context data as produced by
\code{prepare_file()}.}

\item{interval_name}{A character string naming the interval (e.g.
\code{"baseline"}).}

\item{interval_range}{A numeric vector of length 2 giving the start and
end of the interval in the same units as \code{params$time$time_res}.}

\item{params}{A named list of analysis parameters as produced by \code{make_params()}.}
}
\value{
A named list with elements \code{interval_name} (character),
\code{interval_range} (numeric vector), and \code{channels} (a named
list of per-channel results including a combined \code{summary} data frame).
}
\description{
Subsets channel data to a time interval and runs per-channel analysis,
returning results aggregated across all enabled channels.
}
\examples{
\dontrun{
interval_result <- analyze_interval(file_ctx, "baseline", c(0, 24), params)
}
}
