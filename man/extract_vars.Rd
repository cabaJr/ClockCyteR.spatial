% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_foo.R
\name{extract_vars}
\alias{extract_vars}
\title{Extract variables from analysis results across files and save as wide tables}
\usage{
extract_vars(params, file_rows, vars, network = FALSE, individual = FALSE, ...)
}
\arguments{
\item{params}{List of analysis parameters produced by \code{make_params()};
must contain \code{paths$base_dir}, \code{time$intervals}, and
\code{channels}.}

\item{file_rows}{Data frame of file metadata; must contain columns
\code{folder_path} and \code{file_id}.}

\item{vars}{Character vector of column names to extract from each result
table.}

\item{network}{Logical; if \code{TRUE}, reads network result RDS files
instead of period-table RDS files. Defaults to \code{FALSE}.}

\item{individual}{Logical; if \code{TRUE}, saves one row per cell instead
of pivoting to a wide table. Defaults to \code{FALSE}.}

\item{...}{Optional: \code{sub} (character) selects a sub-element from
network results.}
}
\value{
Called for its side effect of writing CSV tables to a \code{tables}
subdirectory; returns \code{NULL} invisibly.
}
\description{
Extract variables from analysis results across files and save as wide tables
}
